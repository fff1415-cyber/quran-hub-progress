<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # PHP API — do not rewrite to SPA
  RewriteRule ^api(/|$) - [L]

  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
